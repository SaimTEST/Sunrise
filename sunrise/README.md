# My Sunrise

A small romantic Streamlit app designed for Streamlit Cloud.

## Run Locally

```bash
pip install -r requirements.txt
streamlit run app.py
```

## Deploy On Streamlit Cloud

Use `app.py` as the main file. The project only requires Streamlit.
